#include <iostream>
#include <vector>

using namespace std;

int main () {
    int counter = 1;
    int n;
    int i;
    while (cin >> n >> i) {
        vector<int> S (n);
        for (int i = 0; i < n; ++i ) cin >> S[i];
        
        int m;
        cin >> m;
        for (int i = 0; i < m; ++i) {
            int a;
            cin >> a;

        }
    }
} 