#include <iostream>
#include <vector>
#include <math.h>

using namespace std;

typedef vector<char> VE;
typedef vector<VE> VVE;
const vector<pair<int, int>> dirs= {{0, 1}, {0, -1}, {1, 0}, {-1, 0}};

int main () {
    int r, c;
    while (cin >> r >> c) {
        
    }
}   